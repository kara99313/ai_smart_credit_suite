# =========================================================
#  AI SMART CREDIT SUITE — .gitignore
#  (Empêche la fuite de données sensibles et fichiers inutiles)
# =========================================================

# --- ENVIRONNEMENT & CLÉS ---
.env
!.env.example       # ← autorise la version publique .env.example
.env.local
.env.*.backup
*.key
*.pem

# --- FICHIERS TEMPORAIRES / CACHE ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.log
*.tmp
*.bak

# --- VENV / DEPENDANCES LOCALES ---
venv/
.venv/
env/
ENV/

# --- CONFIGURATION IDE / ÉDITEURS ---
.vscode/
.idea/
.spyderproject
.spyproject
*.sublime-project
*.sublime-workspace

# --- OS SYSTEM FILES ---
.DS_Store
Thumbs.db
desktop.ini

# --- ARCHIVES / SAUVEGARDES ---
*.zip
*.tar
*.gz
*.rar

# --- RAPPORTS / EXPORTS / DATA ---
data/*.csv
data/*.xlsx
data/*.json
data/*.parquet
data/*.sav
reports/*.pdf
reports/*.html
reports/*.docx
reports/*.txt

# --- MODELES & CHECKPOINTS ML ---
# On ignore les modèles volumineux et temporaires, 
# mais on autorise explicitement le logistic_pipeline_best.pkl
model/*.joblib
model/*.h5
model/*.onnx
model/*.pt
model/*.bin
!model/logistic_pipeline_best.pkl       # ← autorisé (modèle principal)
!model/features.pkl                     # ← autorisé si utilisé
!model/explainer.pkl                    # ← autorisé si utilisé

# --- LOGS / MONITORING ---
logs/
*.out
*.err

# --- NAVIGATION & RAG CACHE ---
vector_store/
docs_rag/index/
*.faiss
*.npy
*.npz

# --- DOCKER / BUILD ---
Dockerfile
docker-compose.yml
.dockerignore
build/
dist/

# --- NOTEBOOKS / TEMP FILES ---
*.ipynb_checkpoints/
*.nbconvert/
*.sqlite
*.db

# --- AUTRES TEMP FILES ---
tmp/
temp/
.cache/
